{
  "name": "@diaas/access-management-node-utils",
  "version": "1.1.10",
  "description": "Assure Platform Access Management Service nodejs utils (SDK)",
  "main": "./src/main.js",
  "scripts": {
    "build": "mkdir -p dist && cp -a ./code/src ./dist && cp package.json ./dist && cp README.md ./dist",
    "test": "jest --silent --watchAll",
    "test-ci": "jest --silent --coverage",
    "test:coverage": "jest --silent --collect-coverage"
  },
  "devDependencies": {
    "aws-sdk": "^2.1277.0",
    "aws-sdk-mock": "^5.8.0",
    "dotenv": "^16.0.3",
    "jest": "^29.3.1",
    "jest-junit": "^15.0.0"
  },
  "author": "aechevarriaf@dxc.com",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "jwt-decode": "^3.1.2"
  }
}
